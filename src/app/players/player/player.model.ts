
export class Player {
  public id: string;
  public name: string;
  public vote: string;

  constructor(id: string, name: string, vote: string) {
    this.id = id;
    this.name = name;
    this.vote = vote;
  }

}